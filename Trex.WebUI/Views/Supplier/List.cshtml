@model IEnumerable<Trex.Entity.Models.Supplier>

@{
    ViewData["Title"] = "GetAll";
}
@if (Model.Count() > 0)
{
    <!-- Content Header (Page header) -->
    <section class="content-header">
        <div class="container-fluid">
            <div class="row">
                <div class="col-12">
                    <div class="card card-info">
                        <div class="card-header">
                            <h3 class="card-title">Shipper List</h3>
                        </div>
                        <!-- /.card-header -->
                        <div class="card-body table-responsive p-0">
                            <table class="table table-hover text-nowrap">
                                <thead>
                                    <tr>
                                        <th>ID</th>
                                        <th>Company Name</th>
                                        <th>Contact Name</th>
                                        <th>Contact Title</th>
                                        @*<th>Street</th>*@
                                        @*<th>City</th>*@
                                        @*<th>Region</th>*@
                                        @*<th>Postal Code</th>*@
                                        <th>Country</th>
                                        <th>Phone</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var supplier in Model.ToList())
                                    {
                                    <tr>
                                        <td>@supplier.Id</td>
                                        <td>@supplier.CompanyName</td>
                                        <td>@supplier.ContactName</td>
                                        <td>@supplier.ContactTitle</td>
                                        @*<td>@supplier.Address.Street</td>*@
                                        @*<td>@supplier.Address.City</td>*@
                                        @*<td>@supplier.Address.Region</td>*@
                                        @*<td>@supplier.Address.PostalCode</td>*@
                                        <td>@supplier.Address.Country</td>
                                        <td>@supplier.Address.Phone</td>

                                        <td><a href="/supplier/update/@supplier.Id" class="btn btn-info btn-xs"><i class="fas fa-edit"></i></a></td>
                                        <td> <a href="/supplier/delete/@supplier.Id" class="btn btn-danger btn-xs"><i class="fas fa-trash"></i></a></td>
                                    </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                        <!-- /.card-body -->
                    </div>
                    <!-- /.card -->
                </div>
            </div><!-- /.container-fluid -->
        </div>
    </section>
}

